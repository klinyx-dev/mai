{
  "name": "@mai/web-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Web packages for consuming mai wasm core.",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm --filter @mai/mai-web-core build && pnpm --filter @mai/mai-wasm-adapter build && pnpm --filter @mai/mai-ui-vue build",
    "example:dev": "pnpm --filter @mai/nuxt-app-example dev",
    "test": "pnpm --filter @mai/mai-web-core test"
  }
}
