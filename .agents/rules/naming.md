# Naming rules

Apply these naming constraints across crates, modules, folders, and generated artifacts:

1. Do not use the `scheduler` prefix unless explicitly requested.
2. The project name is `mai`.
